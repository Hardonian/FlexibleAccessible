# AGENTS.md

## Cursor Cloud specific instructions

This repository ("FlexibleAccessible") is currently an empty scaffold with only a `README.md` and `LICENSE` file. There are no application services, dependencies, build steps, tests, or lint configurations.

- **No package manager or lockfile** exists — no `package.json`, `requirements.txt`, `go.mod`, etc.
- **No services to start** — no dev server, database, or background workers.
- **No tests or lint** — no test framework or linter is configured.

When application code is added, update this section with the relevant dev setup, run, test, and lint instructions.
